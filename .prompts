The multi vault drop down doesn't have any actual functionality. When choosing a vault, I want the UI to update accordingly. I don't want the test vaults added (test/work/personal). If there is a SEECRET and/or JSEECRET environment variables, I want those vaults listed. Also, can you change the "register vault" button to create vault and make it show a directory picker when clicked? The directory picker should allow the user to select a directory for the new vault. Once a directory is selected, the vault should be created and the UI should update to show the new vault in the drop down menu (and the rest of the UI).