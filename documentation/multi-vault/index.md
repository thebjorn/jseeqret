# What if a user can have more than one vault?

In the current design, there is a single vault directory (default: `/srv/.seeqret`) that contains one `seeqret.key` and one `seeqrets.db`. What if the user wants to have multiple vaults, each with its own key and database? For example, one vault for personal secrets and another for work secrets.

